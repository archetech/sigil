{
  "name": "@archetech/sigil-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive demo for Sigil — build a delegation chain and verify it, offline or against a live Archon node",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@didcid/cipher": "^0.4.3",
    "@didcid/clients": "^0.1.3",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "typescript": "^5.6",
    "vite": "^5.4.0"
  }
}
