/** Sigil reference implementation — public surface. */
export { verifyPresentation } from './verify.ts';
export type {
  Jwk,
  Proof,
  Capability,
  AAC,
  VRC,
  Presentation,
  VerifyRequest,
  VerifyResult,
  Resolver,
  ResolvedDid,
  SignatureVerifier,
  VerifyDeps,
} from './types.ts';
